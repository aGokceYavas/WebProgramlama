@{
    ViewData["Title"] = "Ana Sayfa";
}

<style>
    /* HERO BÖLÜMÜ - SİNEMATİK GÖRÜNÜM */
    .hero-wrapper {
        position: relative;
        height: 90vh; /* Neredeyse tam ekran */
        background: url('https://images.unsplash.com/photo-1534438327276-14e5300c3a48?q=80&w=1470&auto=format&fit=crop') no-repeat center center/cover;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        overflow: hidden;
    }

    /* Görselin üzerine koyu filtre atıyoruz ki yazılar okunsun */
    .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to bottom, rgba(0,0,0,0.6), rgba(44, 62, 80, 0.8));
        z-index: 1;
    }

    /* İÇERİK KUTUSU (GLASSMORPHISM - BUZLU CAM) */
    .hero-content {
        position: relative;
        z-index: 2;
        background: rgba(255, 255, 255, 0.1); /* Yarı saydam beyaz */
        backdrop-filter: blur(10px); /* Arka planı bulanıklaştır */
        -webkit-backdrop-filter: blur(10px);
        padding: 3rem 4rem;
        border-radius: 20px;
        border: 1px solid rgba(255, 255, 255, 0.2);
        box-shadow: 0 15px 35px rgba(0,0,0,0.2);
        text-align: center;
        max-width: 800px;
    }

    /* Modern Tipografi */
    .hero-title {
        font-weight: 800;
        font-size: 3.5rem;
        letter-spacing: -1px;
        margin-bottom: 1rem;
        text-shadow: 0 4px 10px rgba(0,0,0,0.3);
    }

    .text-highlight {
        color: transparent;
        background: linear-gradient(90deg, #87CEEB, #4A6C4C); /* Gök Mavisi -> Yosun Yeşili */
        -webkit-background-clip: text;
        background-clip: text;
    }

    /* BUTONLAR */
    .btn-glow {
        background: linear-gradient(45deg, #4A6C4C, #87CEEB);
        border: none;
        color: white;
        padding: 15px 40px;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 50px;
        transition: all 0.3s ease;
        box-shadow: 0 0 20px rgba(135, 206, 235, 0.4); /* Mavi parıltı */
    }

        .btn-glow:hover {
            transform: scale(1.05);
            box-shadow: 0 0 30px rgba(135, 206, 235, 0.6);
            color: white;
        }

    .btn-outline-glass {
        background: transparent;
        border: 2px solid rgba(255,255,255,0.5);
        color: white;
        padding: 13px 40px;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 50px;
        transition: all 0.3s ease;
        margin-left: 15px;
    }

        .btn-outline-glass:hover {
            background: white;
            color: #333;
            border-color: white;
        }

    /* AŞAĞI OK ANİMASYONU */
    .scroll-down {
        position: absolute;
        bottom: 30px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 2;
        color: rgba(255,255,255,0.7);
        animation: bounce 2s infinite;
        font-size: 1.5rem;
    }

    /* KARTLAR BÖLÜMÜ */
    .features-section {
        padding: 80px 0;
        background-color: #f9fbf9;
    }

    .clean-card {
        background: white;
        border-radius: 20px;
        padding: 40px 30px;
        transition: all 0.4s ease;
        border: 1px solid #eee;
        height: 100%;
        position: relative;
        overflow: hidden;
    }

        .clean-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(74, 108, 76, 0.1); /* Yeşilimsi gölge */
            border-color: transparent;
        }

        /* Kartın üzerine gelince alttan çıkan mavi çizgi */
        .clean-card::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0%;
            height: 4px;
            background: linear-gradient(90deg, #4A6C4C, #87CEEB);
            transition: width 0.4s ease;
        }

        .clean-card:hover::after {
            width: 100%;
        }

    .icon-wrapper {
        width: 60px;
        height: 60px;
        border-radius: 15px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.8rem;
        margin-bottom: 25px;
        transition: all 0.3s ease;
    }

    .clean-card:hover .icon-wrapper {
        transform: rotateY(180deg);
    }

    @@keyframes bounce {
        0%, 20%, 50%, 80%, 100% {
            transform: translateY(0) translateX(-50%);
        }

        40% {
            transform: translateY(-10px) translateX(-50%);
        }

        60% {
            transform: translateY(-5px) translateX(-50%);
        }
    }
</style>

<div class="hero-wrapper">
    <div class="hero-overlay"></div>

    <div class="hero-content fade-in-up">
        <span class="text-uppercase fw-bold text-white-50 letter-spacing-2 mb-3 d-block">Profesyonel Spor Deneyimi</span>
        <h1 class="hero-title">
            En İyi Haline <br>
            <span class="text-highlight">Dönüşmeye Hazır Ol</span>
        </h1>
        <p class="lead text-white-50 mb-5 mx-auto" style="max-width: 600px;">
            Yapay zeka destekli analizler, kişiselleştirilmiş programlar ve modern ekipmanlarla hedeflerine sandığından daha yakınsın.
        </p>

        <div class="d-flex justify-content-center flex-wrap gap-2">
            @if (User.Identity.IsAuthenticated)
            {
                <a asp-controller="Randevus" asp-action="Create" class="btn btn-glow">
                    <i class="fa-solid fa-calendar-check me-2"></i>Randevu Al
                </a>
            }
            else
            {
                <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-glow">
                    <i class="fa-solid fa-rocket me-2"></i>Hemen Başla
                </a>
                <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-outline-glass">
                    Giriş Yap
                </a>
            }
        </div>
    </div>

    <div class="scroll-down">
        <i class="fa-solid fa-chevron-down"></i>
    </div>
</div>

<div class="features-section">
    <div class="container">
        <div class="row g-4">
            <div class="col-lg-4 col-md-6 fade-in-up" style="animation-delay: 0.2s;">
                <div class="clean-card">
                    <div class="icon-wrapper" style="background: rgba(74, 108, 76, 0.1); color: #4A6C4C;">
                        <i class="fa-solid fa-dumbbell"></i>
                    </div>
                    <h3 class="h4 fw-bold mb-3">Premium Ekipmanlar</h3>
                    <p class="text-muted mb-0">Son teknoloji kardiyo ve ağırlık ekipmanlarıyla antrenman kaliteni maksimuma çıkar.</p>
                </div>
            </div>

            <div class="col-lg-4 col-md-6 fade-in-up" style="animation-delay: 0.3s;">
                <div class="clean-card">
                    <div class="icon-wrapper" style="background: rgba(135, 206, 235, 0.15); color: #5698c4;">
                        <i class="fa-solid fa-brain"></i>
                    </div>
                    <h3 class="h4 fw-bold mb-3">AI Antrenör</h3>
                    <p class="text-muted mb-0">Yapay zeka asistanımız, vücut analizine göre sana özel haftalık programlar hazırlar.</p>
                </div>
            </div>

            <div class="col-lg-4 col-md-6 fade-in-up" style="animation-delay: 0.4s;">
                <div class="clean-card">
                    <div class="icon-wrapper" style="background: rgba(246, 194, 62, 0.1); color: #f6c23e;">
                        <i class="fa-solid fa-medal"></i>
                    </div>
                    <h3 class="h4 fw-bold mb-3">Uzman Kadro</h3>
                    <p class="text-muted mb-0">Sertifikalı eğitmenlerimizle birebir çalışarak hedeflerine en kısa yoldan ulaş.</p>
                </div>
            </div>
        </div>
    </div>
</div>